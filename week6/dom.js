//Accessing DOM elements
//By ID
const elementID = document.getElementById("elementID");\

//By class name
const byClassName = document.getElementsByClassName("className"); 

//By tag name
const byTagName = document.getElementsByTagName("tagName");

//By query selector(Itas like cessing them by css selector)
// Selects the first element that matches the selector
const byQuerySelectoClass = document.querySelector(".className");  // Selects the first element with the class name
const byQuerySelectorID = document.querySelectorAll("#className"); // Selects all elements with the class name
